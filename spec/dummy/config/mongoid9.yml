development:
  clients:
    default:
      database: doorkeeper-mongoid9-development
      hosts:
        - localhost:27017
      options:
        write:
          w: 1

test:
  clients:
    default:
      database: doorkeeper-mongoid9-test
      hosts:
        - localhost:27017
      options:
        write:
          w: 1
